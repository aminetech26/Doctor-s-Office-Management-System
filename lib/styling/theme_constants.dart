import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';

const primaryColor = Color(0xFF2972FE);
const secondaryColor = Color(0xFF6499FF);
const primaryTextColor = Color(0xFF404345);
const secondaryTextColor = Color(0xFF09101D);
const primaryErrorColor = Color(0xFFDA1414);
const secondaryErrorColor = Color(0xFFFEEFEF);
const backgroundColor = Color(0xFFFFFFFF);

/*
ThemeData defaultTheme = ThemeData(

  colorScheme: const ColorScheme(
    primary: primaryColor,
    error: secondaryTextColor,
    brightness: Brightness.light,
    onPrimary: primaryTextColor,
    secondary: secondaryColor,
    onSecondary: secondaryTextColor,
    onError: primaryErrorColor,
    surface: backgroundColor,
    onSurface: primaryTextColor
  ),
  // Define the text styling using Material Design 3 typography and Source Sans Pro fonts.
  textTheme: TextTheme(
    labelSmall: GoogleFonts.sourceSans3(
      fontSize: size,
      fontWeight: FontWeight.w600, // Semi-bold for displayLarge
    ),
    headlineLarge: GoogleFonts.sourceSans3(
      fontSize: size,
      fontWeight: FontWeight.w600, // Semi-bold for displayLarge
    ),
    headlineMedium: GoogleFonts.sourceSans3(
      fontSize: size,
      fontWeight: FontWeight.w600, // Semi-bold for displayLarge
    ),
    headlineSmall: GoogleFonts.sourceSans3(
      fontSize: size,
      fontWeight: FontWeight.w600, // Semi-bold for displayLarge
    ),
    labelMedium: GoogleFonts.sourceSans3(
      fontSize: size,
      fontWeight: FontWeight.w600, // Semi-bold for displayLarge
    ),
    labelLarge: GoogleFonts.sourceSans3(
      fontSize: size,
      fontWeight: FontWeight.w600, // Semi-bold for displayLarge
    ),
    displayLarge: GoogleFonts.sourceSans3(
      fontSize: size,
      fontWeight: FontWeight.w600, // Semi-bold for displayLarge
    ),
    titleLarge: GoogleFonts.sourceSans3(
      fontSize: size,
      fontStyle: FontStyle.italic,
    ),
    bodyMedium: GoogleFonts.sourceSans3(
      fontSize: size,
      fontWeight: FontWeight.normal, // Regular for bodyMedium
    ),
    bodySmall: GoogleFonts.sourceSans3(
      fontSize: size,
      fontWeight: FontWeight.w600, // Semi-bold for displayLarge
    ),
    bodyLarge: GoogleFonts.sourceSans3(
      fontSize: size,
      fontWeight: FontWeight.w600, // Semi-bold for displayLarge
    ),
    displaySmall: GoogleFonts.sourceSans3(
      fontSize: size,
    ),
  ),
);


 */
