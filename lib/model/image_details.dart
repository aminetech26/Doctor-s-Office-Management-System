import 'package:flutter/material.dart';

class ImageDetails {
  final String imagePath;
  final Color color;
  final String title;
  final String text;

  ImageDetails(this.imagePath, this.color, this.title, this.text);
}